{
  "packages": [
    { "name": "greeter", "version": "1.0.0", "description": "A tiny example Cob package -- prints a greeting and exposes a greetings_given variable." },
    { "name": "math", "version": "1.0.0", "description": "Integer math for Cob -- factorial, gcd, power, abs, min, max, plus a tally op that renders a result as up to 50 '*' lines." }
  ]
}
